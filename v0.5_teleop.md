# Vision Pro 遥操 AR5 + 因时灵巧手

左手控制机械臂末端,右手捏合当离合,五指独立跟踪。

```bash
conda activate dexgraspvla
cd ~/yuhang_workspace/Project_Atom/遥操/VR设备
python avp_servoj_teleop.py <头显IP> --planner spline --lag-ms 50
```

头显 IP 从 Tracking Streamer 界面上读,每次可能变。
回车挂离合,**右手保持捏合**臂才动,松开即停。按 `h` 停下并回位,按 `q` 退出。

---

## 一、简介

每 8 毫秒一个如下循环:

```
读头显手腕位姿 → 滤波 → 算臂的目标位姿 → 逆解成关节角 → 发给控制器
```

控制器收到关节角后,自己把 8 毫秒的间隔插补成 1 毫秒的运动。

端到端延迟约 100 毫秒,其中一半是我们主动加的延迟缓冲(换顺滑)。

---

## 二、为什么用 servoJ

控制器每毫秒需要一个关节角指令。Python 做不到每毫秒准时发一次,晚几毫秒控制器就判通信丢包。

珞石 SDK 给了三条路,只有一条适合遥操:

| 接口 | 语义 | 遥操延迟 |
|---|---|---|
| MoveL | 每个点是一个**任务**,必须走完,撤不回 | 240~500 ms,手越快越差 |
| RT 笛卡尔 | 每毫秒一个位姿 | 这台 AR5 上收指令不动 |
| **servoJ** | 每 8 毫秒一个**关节角目标**,新的盖掉旧的 | **24 ms** |

关键区别是队列。MoveL 发进去的点排队等着被执行,手停了臂还要把队列走完,延迟等于队列深度乘每点执行时间。servoJ 没有队列,发一个盖一个,控制器永远追最新那个。

代价是 servoJ 收关节角不收位姿,逆解要我们自己做。

---

## 三、分步步骤

### 1. 读头显

Tracking Streamer 给左手手腕的 4×4 位姿矩阵,大约每秒 60 帧。

### 2. 滤波

头显位姿自带抖动,不滤的话噪声原样变成关节指令。三级:

- **卡尔曼**(匀速模型):按运动学模型压噪声,顺带估出手速,后面帧间预测要用。
- **One-Euro**:速度自适应低通。手停住时压得死,快速挥动时几乎不引入延迟。普通一阶低通做不到这一点,压得住抖就一定发糊。
- **逆解前再滤一级**:前两级在头显原始位姿上,但之后帧间预测和坐标映射会重新引入噪声,这一级直接滤送进逆解的目标。姿态是取旋转矩阵前两列滤完再 Gram-Schmidt 重建,九个元素分别滤出来不是正交阵。

### 3. 算目标位姿

接合那一刻记下手的位姿和臂末端的位姿作为基准。之后:

```
目标位置 = 基准位置 + 坐标变换 × (手的位移) × 缩放
```

坐标变换是 `--yaw` 和 `--mirror` 合成的。头显系是 X 右、Y 前、Z 上;臂基座系是 X 前、Y 左、Z 上。两者差一个绕 Z 的旋转,现场用 `probe_axes.py` 测一次。

**帧间预测**:头显 60 赫兹而控制周期 125 赫兹,中间两个周期若沿用同一个陈旧目标,后面的速度估计就按错误的间隔算。用卡尔曼估的手速把位置外推到当前时刻。

目标最后钳进工作空间盒。盒子的边角臂到不了,所以各面向内收一点。

### 4. 逆解

AR5 是 7 轴,末端位姿只定 6 个量,多出来的一个自由度是**臂角**,也就是肘绕"肩到腕"这条连线转多少。同一个末端位姿有无数组关节角。

求解分两层:

**闭式解**(`ar5_poe_ik.py`):把臂角当已知量代进去,剩下 6 轴用旋量法加 Paden-Kahan 子问题闭式求出,没有迭代。扫一遍臂角就得到一批候选解。1.9 毫秒。

**选解层**(`ar5_ik_select.py`):求解器只负责给出全部解,挑哪个是策略问题。三道关:

1. **跳变门**:扔掉离上一帧超过 0.25 弧度的候选。同一构型里也有差半个臂角的远解。
2. **构型筛选**:只留和接合时同构型的。用三个比特描述臂处在哪个姿势里 —— 手腕正反、肘朝哪边、肩在前还是在后,组合成八种。同一个末端位姿,不同构型是完全不同的臂型,它们之间没法连续过渡,中间要经过奇异。不筛的话会不知不觉跨过构型边界,臂突然换个姿势。实测一个位姿的 21 个候选分属两个构型。
3. **代价排序**:连续性、关节限位余量、近奇异、臂角偏离锚定值,加权取最小。

合计 3.3 毫秒。

### 5. 插值与发送

逆解只在头显有新帧时更新(约 16.7 毫秒一次),而 servoJ 每 8 毫秒要一个点。中间的点靠插值填。

**延迟缓冲**是这一版顺滑的关键。把逆解结果连时间戳存进缓冲区,输出的是 50 毫秒之前那一刻的值。因为落后了,输出点的两侧都有真实数据,可以做**内插**而不是外推,切线用中心差分。外推只能猜下一个目标在哪,目标一抖速度估计就抖,样条跟着过冲。

发送是一个一个发,servoJ 没有批量接口。

---

## 四、参数

短命令跑的就是下面这套值,只有两个要显式给。

### 必须自己定的

| 参数 | 怎么来的 | 不对会怎样 |
|---|---|---|
| `<头显IP>` | Tracking Streamer 界面上读,每次可能变 | 连不上,卡在等头显 |
| `--yaw` | 现场用 `probe_axes.py` 测,推导值 `-90` | 前后左右整体转 90 度,姿态也一起错 |

### servoJ 三个参数

| 参数 | 默认 | 原理 |
|---|---|---|
| `--period` | 0.008 | 我们对控制器的承诺:多久给一个点。**滞后约等于三倍周期**,实测 8ms→24ms、20ms→60ms、33ms→99ms。太小伺服报错,太大判通信丢包 |
| `--lookahead` | 0.008 | 发送时刻会抖,设了这个值控制器就按固定间隔算速度,抹平抖动。设 0 则按实际到达间隔算,要求发送非常准时 |
| `--kp` | 1.0 | 位置反馈增益。除了按速度插补,再按"上一个目标和当前实际位置的差"补一点。振荡调小,滞后大调大 |

主循环周期、插值周期、这个承诺周期必须是同一个值,否则控制器算出的速度是错的。

### 延迟与顺滑

| 参数 | 默认 | 原理 |
|---|---|---|
| `--lag-ms` | 50 | **这是顺滑度的主旋钮**。落后这么多才能拿到输出点两侧的真实数据做内插。**至少 35**(要覆盖两个头显帧间隔),低于这个值右边没点会退化成保持不动,白搭延迟还更抖。50 是拐点,再往上收益很小 |
| `--target-cutoff` | 4.0 | 逆解前那级滤波的截止频率。对 0.5 赫兹的手部动作,2Hz 滞后 30ms、4Hz 滞后 20ms、6Hz 滞后 15ms。抖就往小调 |
| `--lp-cutoff` | 3.0 | 头显位姿那级滤波。同上取舍。三处滤波的滞后是叠加的,要调先调 `--target-cutoff` |

### 速度与安全

| 参数 | 默认 | 原理 |
|---|---|---|
| `--max-vel` | 1.2 rad/s | 插值的关节速度上限。手快到超过它时臂会落后,幅值被削 |
| `--max-acc` | 8.0 rad/s² | 加速度上限 |
| `--max-step-rad` | 0.03 | 单帧每关节最大变化量的硬上限,规划之外的最后一道网。臂永远不会跳 |
| `--ws-margin-mm` | 20 | 工作空间盒 x/y 各面向内收。盒子边角臂到不了,撞上就是执行错误 |
| `--ws-margin-z-mm` | 10 | z 方向收得少,收多了臂"下不去" |

工作空间盒本身按实测可达范围定过:从起始位姿保持姿态单轴外推,x 到 `[0.265, 0.645]`、
y 到 `[-0.369, 0.396]`、z 到 `[-0.305, 0.470]` 米。盒子上界取各轴极限再往里 15~20mm,
下界不动 —— z 下界是台面保护。盒子比可达集大是正常的,可达集接近球壳,方盒总会外溢,
溢出的部分由逆解拒绝。

### 手感

| 参数 | 默认 | 原理 |
|---|---|---|
| `--scale` | 0.4 | 手动 1 米末端动 0.4 米。臂的工作空间竖直方向只有 ±170mm,而人手自然行程约 ±400mm,不缩放会先用光臂的行程 |
| `--rot-mode` | roll | 只跟手掌绕前臂的拧转。`full` 是完整姿态跟随,但腕关节只有 ±50 度,很容易够不到 |
| `--w-psi` | 0.15 | 选解代价里臂角偏离的权重。大则肘钉死,小则更跟手但肘会慢慢游移。想更稳就 0.5 |
| `--ik-max-jump` | 0.25 rad | 逆解跳变门 |

### 调参顺序

1. **抖** → `--target-cutoff` 降到 2 或 3;还抖 → `--lag-ms` 加到 80。
2. **滞后** → `--lag-ms` 降到 40(不能低于 35);还慢 → `--target-cutoff` 升到 6。
3. **肘乱动** → `--w-psi` 升到 0.5。
4. **快动作跟不上,幅值被削** → `--max-vel` 升到 2.0,`--max-acc` 升到 15。
5. **够不到,老是撞工作空间** → `--scale` 降到 0.3。

每次只动一个,跑完看退出统计里的端到端延迟对比。

---

## 五、出问题看哪里

| 症状 | 原因 |
|---|---|
| 连不上头显,errno 11 | Tracking Streamer 退到后台或头显休眠。切回前台,确认界面上的 IP |
| `电机上不了电` | 上一次退出 RT 模式后掉电,控制器还没准备好。程序会重试 6 次;还不行等 40 秒再跑 |
| 手一个字节都不回 | 手的控制循环半死。**拔手自己的 24V 电源**,等 5 秒再上,不是拔 USB。先用 `test_fingers.py` 单独验证 |
| 手的串口找不到 | 拔插后设备号会变。用 by-id 路径:`/dev/serial/by-id/usb-1a86_USB_Serial-if00-port0` |
| 正解自检差超过 5mm | 本地运动学和控制器对不上,映射基准会错,程序会自己停。查 `ar5_dh.json` |
| 前后左右整体转了 90 度 | `--yaw` 不对,跑 `probe_axes.py` 定值 |
| 左右反了但前后是对的 | 这是反射不是旋转,yaw 修不了,加 `--mirror lr` |
| 逆解失败计数在涨 | 目标超出可达范围。手回到工作空间中间重新捏合 |
| 臂"下不去" | `--ws-margin-z-mm` 设太大 |

跑起来就有运行日志写在 `logs/servoj_<时间>.log`,里面有每秒一行的快照和退出统计。

---

## 六、文件

| 文件 | 作用 |
|---|---|
| `avp_servoj_teleop.py` | **主程序**,遥操就跑这一个 |
| `ar5_poe_ik.py` | 旋量法闭式逆解 |
| `ar5_ik_select.py` | 选解层:跳变门、构型筛选、代价排序 |
| `ar5_sew.py` | 构型三比特判别、立体投影臂角 |
| `hand_retarget.py` | 25 关节手骨架 → 手的 6 维 |
| `inspire_hand6.py` | 灵巧手驱动 |
| `home_arm.py` | 单独把臂送回起始姿态 |
| `probe_axes.py` | 定 `--yaw` / `--mirror`,不碰臂 |
| `test_fingers.py` | 逐根手指张合,只碰手 |
| `probe_servoj.py` | 量 servoJ 的滞后和稳定性,不连头显 |

对照用的其他方案:`avp_movel_teleop.py` 走控制器 MoveL,`avp_arm_teleop.py --arm-backend rt` 走本地 1kHz 关节流。两者的延迟都明显更大,留着做对比。

在这套遥操上面采数据见 [`docs/数据采集.md`](docs/数据采集.md);三条路各自是怎么试出来的、
在哪儿撞的墙,见 [`docs/AR5遥操三条路.md`](docs/AR5遥操三条路.md)。

## 七、效果对比

五段都是同一只手、同一段动作,只换臂这一侧的方案。视频在
[`data/uploads/v0.5/teleop/`](data/uploads/v0.5/teleop/),点开链接在 GitHub 里直接播放。

**1.  servoJ（第一版）：**
没有插值和规划、原始闭式解没有选解层、没有帧间预测、没有逆解前滤波
▶ [normal_video1.mp4](data/uploads/v0.5/teleop/20260921_170144_normal_video1.mp4)

**2.  Nrt（MoveL 队列式批量发送）：**
控制器队列里维持最多 10 个点,剩到 3 个时一次补 4 个。这一版延迟最大,能看到臂在重放你几秒前的动作。
▶ [normal_video2.mp4](data/uploads/v0.5/teleop/20260921_170423_normal_video2.mp4)

**3.  Nrt（MoveL 单点发送）：**
不攒本地队列,一个点一条命令,控制器里最多一个点没走完。延迟比上一条小很多,但臂每到一个点就停一下,一段一段的。
▶ [normal_video3.mp4](data/uploads/v0.5/teleop/20260921_170526_normal_video3.mp4)

**4.  RT（本地 IK 加关节实时流）：**
只做了One-Euro滤波，采用闭式解,挑离上一帧最近的，Python 自己做插补。抖动严重，低延迟，跟手性不稳定。
▶ [normal_video4.mp4](data/uploads/v0.5/teleop/20260921_170745_normal_video4.mp4)

**5.  servoJ（最终版）：**
滤波采用跳变门 + 卡尔曼 + One-Euro,逆解前再滤一级，逆解采用闭式解 + 选解层(跳变门、构型筛选、代价排序)，控制器自己做插补。延迟100ms，正常速度无抖动，延迟/抖动/跟手性平衡最好
▶ [normal_video5.mp4](data/uploads/v0.5/teleop/20260921_171009_normal_video5.mp4)